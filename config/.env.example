# Shared environment variables for docker compose and Make targets.
# Copy to the repository root as `.env` before running infra commands.

COMPOSE_PROJECT_NAME=ocr-review

# Networking
FDB_EXTERNAL_PORT=4500
BACKEND_PORT=8080
CLIENT_PORT=4173
KESTRA_UI_PORT=8081

# Service configuration
FDB_CLUSTER_FILE=./config/foundationdb/fdb.cluster
CLIENT_BASE_URL=http://localhost:${CLIENT_PORT}
KESTRA_CONFIG_PATH=./config/kestra/application.yaml
KESTRA_SECRETS_PATH=./config/kestra/secrets.yml
