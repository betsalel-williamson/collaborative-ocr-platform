# ============================================================================
# SECURITY WARNING: DUMMY/EXAMPLE CREDENTIALS - LOCAL DEVELOPMENT ONLY
# ============================================================================
# This file contains DUMMY/EXAMPLE credentials for LOCAL DEVELOPMENT ONLY.
#
# ❌ DO NOT use these credentials in:
#    - Staging environments
#    - Production environments
#    - Any non-locally hosted environments (cloud, remote servers, etc.)
#
# ✅ ONLY use these credentials for:
#    - Local development on your personal machine
#
# DO NOT commit real credentials to version control.
#
# To use this file for local development:
# 1. Copy this file to config/kestra/secrets.yml (which is gitignored)
# 2. For local dev: You can use the DUMMY values as-is
# 3. For staging/production: Replace all DUMMY values with real credentials
#    from a secrets management system
# 4. Never commit config/kestra/secrets.yml to git
#
# ============================================================================

kestra:
  datasources:
    main:
      # DUMMY VALUE - Replace with real database username in production
      username: kestra
      # DUMMY VALUE - Replace with real database password in production
      password: kestra
  foundationdb:
    # DUMMY PATH - Update to production cluster file path if needed
    cluster_file: ./config/foundationdb/fdb.cluster

